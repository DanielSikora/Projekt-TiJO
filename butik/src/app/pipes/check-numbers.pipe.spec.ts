import { CheckNumbersPipe } from './check-numbers.pipe';

describe('CheckNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
